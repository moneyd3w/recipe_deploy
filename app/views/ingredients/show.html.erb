<p style="color: green"><%= notice %></p>
  <div class="btn-group">
    <%= link_to "Back to Other Recipes", ingredients_path, class: "btn btn-primary" %>
  </div>
<%= render @ingredient %>

<div>

  <%if  @user != nil && @user.id == @ingredient.user_infos_id%>
    <div class="btn-group">
      <%= link_to "Edit Recipe", edit_ingredient_path(@ingredient), class: "btn btn-warning" %>
      <%= button_to "Delete this Recipe", @ingredient, method: :delete, class: "btn btn-danger"%>
    </div>

  <%end%>
</div>
